version: 1
frontend:
  phases:
    build:
      commands:
        # No npm build required for Zero-Bundler architecture
        - echo "Skipping build phase..."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []